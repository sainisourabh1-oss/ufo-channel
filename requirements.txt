# Script generation (free tier)
google-generativeai>=0.8

# Narration (Google Cloud Text-to-Speech — Chirp 3: HD Hindi)
google-cloud-texttospeech>=2.16

# Captions (align known TTS text -> timed SRT; CPU-light)
faster-whisper>=1.0

# YouTube upload + Chronicling America / war.gov fetching
google-api-python-client>=2.130
google-auth>=2.30
google-auth-oauthlib>=1.2
requests>=2.32

# Media + data
Pillow>=10.0          # image prep for Ken Burns stills
PyYAML>=6.0
# ffmpeg itself is installed by the workflow (apt), not pip
